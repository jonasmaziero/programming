//------------------------------------------------------------------------------
#include <stdio.h>
#include <stdlib.h>
#include <complex.h>
//------------------------------------------------------------------------------
// Remarks
// - all commands in between /*  */ are ignored by the compiler
//------------------------------------------------------------------------------
int main()
{
  int j,k;
  double _Complex A[2][2], B[2][2], C[2][2];
  A[0][0] = 1; A[0][1] = 1; A[1][0] = 1; A[1][1] = 1;
  B[0][0] = 2; B[0][1] = 3; B[1][0] = 4; B[1][1] = 5;

  // C = A+I*B; // Não funciona!

  for(j=0; j<2; j++){
    for(k=0; k<2; k++){
      C[j][k] = A[j][k] + I*B[j][k];
      printf("%lf + I*%lf\n", creal(C[j][k]), cimag(C[j][k]));
    }
  }
  return 0;
}
//------------------------------------------------------------------------------
