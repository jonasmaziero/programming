#include "header.h"  /* Include the header (not strictly necessary here) */

int foo1(int x)    /* Function definition */
{
    return x + 1;
}

int foo2(int x)    /* Function definition */
{
    return x + 2;
}
